language: ruby
sudo: false
rvm:
  - 2.3.3

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true

install: gem install bundler html-proofer && bundle install

script: bundle exec jekyll build && htmlproofer ./_site

notifications:
  email: false